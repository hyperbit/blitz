<% if !flash[:notice].blank? %>
  <div class="alert alert-info">
  <%= flash[:notice] %>
  </div>
<% end %>
<br />
<%= link_to "New Ebook", new_ebook_path, class: "btn btn-primary" %>
<br />
<br />
<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Download Link</th>
      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
      <tr>
        <td><%= @ebook.title %></td>
        <td><%= link_to "View Ebook", ebook_path(@ebook, :job_id => @job_id) %></td>
      </tr>
  </tbody>
</table>
